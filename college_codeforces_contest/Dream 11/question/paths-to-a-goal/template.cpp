#include <bits/stdc++.h>
using namespace std;

int paths_to_a_goal(string str, int lim, int start, int finish)
{
}

int main()
{
	string str;
	cin >> str;

	int lim, start, finish;

	cin >> lim;
	cin >> start >> finish;

	cout << paths_to_a_goal(str, lim, start, finish) << endl;
	return 0;
}
